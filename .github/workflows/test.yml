name: Test

on:
  push:
    branches: [ main, feature/*, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install eclipse-zenoh>=1.9.0 apache-iotdb>=2.0.0
        pip install -r requirements.txt
        pip install pytest pytest-asyncio httpx
    
    - name: Run unit tests
      run: |
        python -m pytest tests/ -v --tb=short
    
    - name: Run linting
      run: |
        pip install "pylint==4.0.6"
        pylint app/ tests/ --disable=C0114,C0115,C0116,W0613,W0621,W0212,C0103,W0603,R1732,C0415,R0914,R0801,W1404

    - name: Check code formatting
      run: |
        pip install "black==26.5.1"
        black --check app/ tests/
